# README - NixOS с Hyprland

🎯 **Простая установка конфигураций NixOS + Hyprland с пользователем `lav`**

## 🚀 Быстрый старт

**Одна команда для установки конфигураций:**
```bash
sudo ./install-configs.sh
```

**Что делает скрипт:**
- ✅ Определяет VirtualBox автоматически и применяет оптимизации
- ✅ Создает бэкапы существующих конфигураций
- ✅ Копирует системную конфигурацию (`configuration.nix`) в `/etc/nixos/`
- ✅ **Автоматически применяет конфигурацию** (`nixos-rebuild switch`)
- ✅ Создает пользователя `lav` с паролем `lav`
- ✅ Устанавливает пользовательские конфигурации Hyprland и Waybar
- ✅ Исправляет проблемы с кодировкой и шрифтами
- ✅ Создает все необходимые директории

**Важно:** Скрипт НЕ изменяет `hardware-configuration.nix`. Для его создания используйте:
```bash
sudo nixos-generate-config
```

**После установки:**
1. **Перезагрузитесь**: `sudo reboot`
2. **Войдите как пользователь**: `lav`
3. **Смените пароль**: `passwd`

⚠️ **НЕ НУЖНО** запускать `nixos-rebuild switch` - скрипт уже это сделал!

## Файлы конфигурации

### 🔧 Системные конфигурации
- **`configuration.nix`** - Основная конфигурация NixOS с пользователем lav, шрифтами и UTF-8

### 🎨 Пользовательские конфигурации  
- **`hyprland.conf`** - Стандартная конфигурация Hyprland window manager
- **`hyprland-virtualbox.conf`** - VirtualBox-оптимизированная конфигурация Hyprland
- **`waybar-config.json`** и **`waybar-style.css`** - Конфигурация и стили панели Waybar
- **`kitty.conf`** - Конфигурация терминала с исправлениями кодировки UTF-8

## Установка

**⚠️ ВАЖНО: Проверьте совместимость версий!**
Эта конфигурация предназначена для NixOS 24.05+ и полностью совместима с NixOS 25.05+.

**🔧 Автоматическая обработка совместимости:**
- NixOS 24.05+: Используются стандартные опции
- NixOS 25.05+: Автоматически удаляются устаревшие опции
- Подробности в файле `NIXOS-25-COMPATIBILITY.md`

Проверить версию: `nixos-version`

## Быстрая установка

**Одна команда для установки:**
```bash
sudo ./install-configs.sh
```

## Ручная установка (опционально)

Если хотите установить конфигурации вручную:

1. **Создайте конфигурацию оборудования:**
   ```bash
   sudo nixos-generate-config
   ```

2. **Скопируйте системную конфигурацию:**
   ```bash
   sudo cp configuration.nix /etc/nixos/
   ```

3. **Примените конфигурацию:**
   ```bash
   sudo nixos-rebuild switch
   ```
   Пользователь `lav` с паролем `lav` будет создан автоматически.

4. **Скопируйте пользовательские конфигурации:**
   ```bash
   mkdir -p ~/.config/hypr ~/.config/waybar
   cp hyprland.conf ~/.config/hypr/
   cp waybar-config.json ~/.config/waybar/config
   cp waybar-style.css ~/.config/waybar/style.css
   ```

## Основные горячие клавиши

- `Super + Enter` - Открыть терминал (Kitty)
- `Super + R` - Запустить приложение (Rofi)
- `Super + Q` - Закрыть окно
- `Super + M` - Выйти из Hyprland
- `Super + V` - Переключить плавающий режим
- `Super + E` - Файловый менеджер (Thunar)
- `Super + 1-9` - Переключиться на рабочий стол
- `Super + Shift + 1-9` - Переместить окно на рабочий стол
- `Super + Print` - Скриншот всего экрана
- `Super + Shift + S` - Скриншот области
- `Super + стрелки` - Перемещение фокуса между окнами
- `Super + Shift + стрелки` - Перемещение окон

## Дополнительные настройки

### Обои
```bash
# Установите feh или swww для обоев
nix-shell -p feh
feh --bg-scale /path/to/wallpaper.jpg
```

### Автоматический вход
По умолчанию включен автоматический вход для пользователя `lav`. Отключите в `configuration.nix` если не нужно:
```nix
# Закомментируйте эту строку:
# services.getty.autologinUser = "lav";
```

### Смена пароля
После первого входа смените пароль:
```bash
passwd
```

### Дополнительные пакеты
Добавьте нужные пакеты в список `environment.systemPackages` в `configuration.nix`.

## VirtualBox

Скрипт автоматически определяет VirtualBox и применяет соответствующие оптимизации для лучшей производительности.

## Часто задаваемые вопросы (FAQ)

### ❓ Нужно ли запускать `nixos-rebuild switch` после скрипта?
**НЕТ!** Скрипт `install-configs.sh` автоматически выполняет `nixos-rebuild switch`. Просто перезагрузитесь после завершения скрипта.

### ❓ Что делать если скрипт завершился с ошибкой?
1. Проверьте синтаксис конфигурации: `sudo nixos-rebuild dry-build`
2. Запустите диагностику: `./diagnose-config.sh`
3. Попробуйте снова: `sudo ./install-configs.sh`

### ❓ Как изменить пользователя с `lav` на другой?
Отредактируйте `configuration.nix`, замените `lav` на ваше имя и запустите `sudo nixos-rebuild switch`.

### ❓ Работает ли это в VirtualBox?
Да! Скрипт автоматически определяет VirtualBox и применяет соответствующие оптимизации.

### ❓ Что если нужна конфигурация оборудования?
Скрипт НЕ управляет `hardware-configuration.nix`. Создайте её командой: `sudo nixos-generate-config`

## Обновление системы

```bash
sudo nixos-rebuild switch
```

## Полезные команды

- `nixos-option` - Просмотр опций конфигурации
- `nix search` - Поиск пакетов
- `nix-collect-garbage` - Очистка старых поколений
- `nixos-rebuild switch --rollback` - Откат к предыдущей конфигурации

## Дополнительная документация

- **`INSTALL-GUIDE.md`** - Подробное руководство по установке
- **`NIXOS-25-COMPATIBILITY.md`** - Совместимость с NixOS 25.05+ и автоматические исправления
- **`ENCODING-FIX.md`** - Решение проблем с кодировкой и отображением символов

## Дополнительные скрипты

- **`diagnose-config.sh`** - Диагностика системы и конфигурации
- **`fix-config.sh`** - Исправление конфигурации NixOS
- **`fix-encoding.sh`** - Исправление проблем с кодировкой
- **`test-encoding.sh`** - Тест отображения UTF-8 символов

## Troubleshooting

### Проблемы с кодировкой (символы-квадратики)
Если символы в терминале отображаются как квадратики:

```bash
# Тест отображения символов
./test-encoding.sh

# Автоматическое исправление
sudo ./fix-encoding.sh
```

Скрипт `install-configs.sh` автоматически решает проблемы с кодировкой.

### Синтаксические ошибки
Если при применении конфигурации возникают ошибки:

```bash
# Проверка конфигурации
sudo nixos-rebuild dry-build

# Автоматическое исправление
sudo ./fix-config.sh

# Диагностика
./diagnose-config.sh
```

### Частые проблемы

1. **Проблемы с графикой:** Проверьте настройки `hardware.graphics` в конфигурации
2. **Не работает звук:** Убедитесь, что PipeWire запущен: `systemctl --user status pipewire`
3. **Проблемы с клавиатурой:** Проверьте настройки раскладки в Hyprland и X11

Для получения дополнительной помощи обратитесь к документации NixOS и Hyprland.
